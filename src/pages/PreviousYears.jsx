import { Accordion } from './../components';

const PreviousYears = ({
  returns22,
  returns23
}) => {
  return (
    <Accordion
      items={
        []
      }
    />
  )
} 

export default PreviousYears;